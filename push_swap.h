#ifndef PUSH_SWAP_H
# define PUSH_SWAP_H

//all stuff to include
# include "libft/libft.h"

//all structs

typedef struct s_stack
{
    int				data;
    struct s_stack	*next;
}					t_stack;

//all prototypes

#endif