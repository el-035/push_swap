with cur i mean the first element of the stack
with next the second
with prev the last


algorithm function
	if stack a is sorted
		return stack a
	pb
	pb
	while stack a is not empty and stack b is not sorted
		if cur a > than next and prev
			if cur b is bigger than next bigger
				sb
			pb
		else if next a is the biggest of the three
			if cur b is bigger than next b
				ss
			else
				sa 
			pb
		else if prev a is biggest
			if cur b bigger than next b
				sb
			rra
			pb

		if b is not sorted
			if cur b bigger than prev b
				rb
			if cur b smaller than prev b 
				while cur b is not smaller than next b and bigger than prev b
					rrb
					sb
				while b is not sorted 
					rb
			
